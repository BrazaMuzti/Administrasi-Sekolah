# Dependensi npm
node_modules/

# Login Google Apps Script (clasp) — simpan kredensial di ~/, jangan di repo
.clasprc.json
~/.clasprc.json
.clasp.json.local

# Supabase CLI (state lokal & kredensial)
supabase/.temp/
supabase/.branches/

# Artefak verifikasi deno (lockfile root dihasilkan saat deno check)
deno.lock

# Konfigurasi tool lokal per mesin
opencode.json

# Log & OS
npm-debug.log*
.DS_Store
Thumbs.db

# Hasil kompilasi CSS (aktifkan bila build dilakukan via CI saja)
# web/css/style.css